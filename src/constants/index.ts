import { DATE_FORMAT_OPTIONS } from "./date";
import { ROUTES } from "./routes";
import SYSTEM_ABOUT from "./system-about";

const constants = {
    DATE_FORMAT_OPTIONS,
    SYSTEM_ABOUT,
    ROUTES,
};

export default constants;
