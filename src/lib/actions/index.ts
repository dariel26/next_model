import { createOneCollectionBySpreadsheets } from "./collection";
import { createOneModel } from "./model";

const actions = { createOneModel, createOneCollectionBySpreadsheets };

export default actions;
